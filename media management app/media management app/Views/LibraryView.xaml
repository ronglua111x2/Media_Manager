<UserControl x:Class="media_management_app.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#ffffff">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="560">
            <TextBlock Text="{Binding Title}" FontSize="30" FontWeight="SemiBold" HorizontalAlignment="Center" />
            <TextBlock Text="{Binding Summary}"
                       Foreground="#64748b"
                       FontSize="15"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Margin="0,12,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
