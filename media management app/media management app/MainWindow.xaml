<Window x:Class="media_management_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:media_management_app.ViewModels"
        mc:Ignorable="d"
        Title="Media Manager" Height="720" Width="1120"
        MinHeight="560" MinWidth="920">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,12" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="12,7" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="24" MaxWidth="820" HorizontalAlignment="Left">
                    <TextBlock Text="Settings" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20" />

                    <TextBlock Text="State folder" FontWeight="SemiBold" />
                    <TextBox Text="{Binding StateFolder, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="Source folders, one per line" FontWeight="SemiBold" />
                    <TextBox Text="{Binding SourceFoldersText, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             MinHeight="110"
                             VerticalContentAlignment="Top"
                             VerticalScrollBarVisibility="Auto" />

                    <TextBlock Text="Output library folder" FontWeight="SemiBold" />
                    <TextBox Text="{Binding OutputLibraryFolder, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="TMDb API Read Access Token" FontWeight="SemiBold" />
                    <TextBox Text="{Binding TmdbReadAccessToken, UpdateSourceTrigger=PropertyChanged}" />

                    <StackPanel Orientation="Horizontal">
                        <Button Content="Save Settings" Command="{Binding SaveCommand}" />
                    </StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" Margin="0,16,0,0" Foreground="#2f6f3e" />
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:InboxViewModel}">
            <DockPanel Margin="24">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
                    <Button Content="Scan Sources" Command="{Binding ScanCommand}" />
                    <Button Content="Create Hardlink" Command="{Binding LinkSelectedCommand}" />
                    <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="8,0,0,0" />
                </StackPanel>

                <DataGrid ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="State" Binding="{Binding State}" Width="90" />
                        <DataGridTextColumn Header="Show" Binding="{Binding ShowTitle}" Width="180" />
                        <DataGridTextColumn Header="Season" Binding="{Binding SeasonNumber}" Width="70" />
                        <DataGridTextColumn Header="Episode" Binding="{Binding EpisodeNumber}" Width="75" />
                        <DataGridTextColumn Header="Source Path" Binding="{Binding FilePath}" Width="*" />
                        <DataGridTextColumn Header="Linked Path" Binding="{Binding LinkedPath}" Width="260" />
                        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="220" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ReviewViewModel}">
            <Grid Margin="24">
                <TextBlock Text="{Binding Message}" FontSize="20" />
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Left" Width="180" Background="#f3f3f3" BorderBrush="#d0d0d0" BorderThickness="0,0,1,0">
            <StackPanel Margin="12">
                <TextBlock Text="Media Manager" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16" />
                <Button Content="Inbox" Command="{Binding ShowInboxCommand}" Margin="0,0,0,8" />
                <Button Content="Review" Command="{Binding ShowReviewCommand}" Margin="0,0,0,8" />
                <Button Content="Settings" Command="{Binding ShowSettingsCommand}" Margin="0,0,0,8" />
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Height="170" Background="#111111" BorderBrush="#303030" BorderThickness="0,1,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="Console Log"
                           Foreground="#d6d6d6"
                           FontWeight="SemiBold"
                           Margin="10,8,10,4" />
                <ListBox ItemsSource="{Binding UiLogs}"
                         Background="#111111"
                         BorderThickness="0"
                         Foreground="#d6d6d6"
                         FontFamily="Consolas"
                         FontSize="12"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="NoWrap" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <ContentControl Content="{Binding CurrentView}" />
    </DockPanel>
</Window>
